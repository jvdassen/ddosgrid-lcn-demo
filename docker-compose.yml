version: '3'
services:
  ddosgridapi:
    ports:
      - "8080:8080"
    build: "."
    volumes:
      - userdata:/usr/src/app/api/data/
    command:
      - /usr/src/app/api/scripts/start_demo.sh
  ddosgridfrontend:
    ports:
      - "8081:8081"
    build: "./frontend"
    command: npm run serve
volumes:
  userdata:
